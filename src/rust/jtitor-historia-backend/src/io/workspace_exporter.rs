/*!
 * Defines the WorkspaceExporter type.
*/
use crate::model::Workspace;

pub struct WorkspaceExporter {}

impl WorkspaceExporter {
    /**
     * Writes the given workspace to a new
     * workspace file.
     */
    pub fn export(&self, workspace: &Workspace) {
        unimplemented!()
    }
}
